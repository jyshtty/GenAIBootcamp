# EPAM DIAL API key (obtain from EPAM Support)
DIAL_API_KEY=your_dial_api_key_here

# URL to extract content from (copy chunks.json from basic-rag if needed)
TARGET_URL=https://example-website.com

# Max number of message pairs to keep in context (for trimming)
MAX_HISTORY_LENGTH=10
